# Use pnpm shamefully-hoist for better compatibility
shamefully-hoist=true

# Enable strict peer dependencies
strict-peer-dependencies=false

# Allow legacy peer dependency resolution for npm
legacy-peer-deps=true

# Use lockfile from workspace
lockfile=true
